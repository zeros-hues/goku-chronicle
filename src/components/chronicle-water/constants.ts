// Wave simulation grid (fixed size, maps to 100% canvas via UV)
export const GW = 200; // grid width  (cells)
export const GH = 160; // grid height (cells)

// Cursor injection
export const CURSOR_RADIUS  = 7;   // grid cells
export const CURSOR_MIN_SPD = 1.5; // px/frame min speed to inject
